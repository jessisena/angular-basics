import { Component } from '@angular/core';

@Component({
  selector: 'app-users',
  template: `
  <p>Im a user component</p>
  `
})

export class UsersComponent {
    text = 'This is a users component';
}